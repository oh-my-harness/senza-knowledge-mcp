# M4 管理后台(文档入库 + 知识库查看)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 提供 FastAPI 管理后台:浏览器上传 PDF/文本入库,查看知识库内容。

**Architecture:** FastAPI 独立进程,轻量 HTML 页面(服务端渲染)+ REST API 底座。上传走 Docling 解析 → RawStore 入库;查看走 RawStore.list/read_document。与 M3 MCP 服务共享原始层目录、独立进程。

**Tech Stack:** Python 3.12、FastAPI 0.141、uvicorn、RawStore(M1)、create_parser("docling")(M1)、jinja2(轻量模板或内联 HTML)、pytest + TestClient。

**Spec:** `docs/M4-管理后台-spec.md`

## Global Constraints

- 独立 FastAPI 进程,不并入 MCP 服务。
- 轻量 HTML 页面 + `/api/*` JSON 底座;不引入 React/Vite。
- 复用 M1:`RawStore(list/read_document/store)`、`create_parser("docling")`、`Settings.raw_dir`。
- 与 M3 共享原始层 raw/ 目录。
- 上传仅接受 PDF + UTF-8 文本,拒绝其他类型。
- 归纳审批(M6)不在 M4。
- 本地单用户,不做鉴权。
- 测试全绿为完成任务标准。

---

### Task 1: admin_routes.py(路由 + 入库逻辑)

**Files:**
- Create: `src/senza_knowledge_mcp/admin_routes.py`
- Test: `tests/test_admin_routes.py`

**Interfaces:**
- Consumes: `RawStore`(M1: list/read_document/store/read_source)、`create_parser("docling")`(M1)
- Produces:
  - `router = APIRouter()`
  - `def is_supported(filename: str) -> bool`
  - `def read_upload_to_bytes(file: UploadFile) -> bytes`
  - `@router.post("/api/upload") async def upload(file: UploadFile, raw: RawStore) -> dict`
  - `@router.get("/api/sources") def sources(raw: RawStore) -> list[dict]`
  - `@router.get("/api/sources/{source_id}") def source_detail(source_id: str, raw: RawStore) -> dict`

- [ ] **Step 1: 写失败测试(用真实 RawStore + Text 文本,不触发 Docling 慢路径)**

```python
# tests/test_admin_routes.py
import tempfile
from pathlib import Path
import pytest
from fastapi.testclient import TestClient

from senza_knowledge_mcp.admin_app import create_app
from senza_knowledge_mcp.ingest.raw_store import RawStore


def test_is_supported():
    from senza_knowledge_mcp.admin_routes import is_supported
    assert is_supported("a.pdf")
    assert is_supported("b.txt")
    assert is_supported("c.md")
    assert not is_supported("d.png")
    assert not is_supported("e.exe")


def make_app(tmp_path):
    raw = RawStore(tmp_path / "raw")
    return create_app(tmp_path / "raw")


def test_upload_text(tmp_path):
    app = make_app(tmp_path)
    c = TestClient(app)
    r = c.post("/api/upload", files={"file": ("hello.txt", b"# Hello\n\ndiffraction sinc kernel", "text/plain")})
    assert r.status_code == 200
    body = r.json()
    assert body["source_id"]
    assert body["name"] == "hello.txt"


def test_upload_rejects_non_supported(tmp_path):
    app = make_app(tmp_path)
    c = TestClient(app)
    r = c.post("/api/upload", files={"file": ("x.png", b"\x89PNG", "image/png")})
    assert r.status_code == 400


def test_list_sources(tmp_path):
    raw = RawStore(tmp_path / "raw")
    from senza_knowledge_mcp.ingest.parser import ParseResult
    raw.store("a.md", b"a", ParseResult(document_markdown="# A\n\ncontent", images=[]))
    app = create_app(tmp_path / "raw")
    c = TestClient(app)
    r = c.get("/api/sources")
    assert r.status_code == 200
    items = r.json()
    assert any(s["origin"] == "a.md" for s in items)


def test_source_detail(tmp_path):
    raw = RawStore(tmp_path / "raw")
    from senza_knowledge_mcp.ingest.parser import ParseResult
    stored = raw.store("a.md", b"a", ParseResult(document_markdown="# A\n\ncontent here", images=[]))
    app = create_app(tmp_path / "raw")
    c = TestClient(app)
    r = c.get(f"/api/sources/{stored.source_id}")
    assert r.status_code == 200
    assert "content here" in r.json()["document"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_admin_routes.py -v`
Expected: FAIL,`ModuleNotFoundError: senza_knowledge_mcp.admin_app`(admin_app 尚未建,Tesk 1 路由由 Task 2 挂载;先建 admin_routes 最小,admin_app 在 Task 2)

> 说明:测试 import `create_app`(Task 2)。为让 Task 1 可独立验证,Task 1 先实现 `admin_routes.router` + 辅助函数 + `single_source_dir_app`(最小 app:仅挂 admin_routes,不含页面)。Task 2 加 create_app(含页面)。

- [ ] **Step 3: 写实现**

```python
# src/senza_knowledge_mcp/admin_routes.py
"""管理后台 API 路由:文档上传入库 + 知识库查看."""
from __future__ import annotations

from pathlib import Path

from fastapi import APIRouter, Depends, HTTPException, UploadFile

from senza_knowledge_mcp.ingest import create_parser
from senza_knowledge_mcp.ingest.raw_store import RawStore

router = APIRouter()

_SUPPORTED = {".pdf", ".txt", ".md"}


def is_supported(filename: str) -> bool:
    return Path(filename).suffix.lower() in _SUPPORTED


def get_raw(raw_dir: Path):
    def _dep():
        return RawStore(raw_dir)
    return _dep


def single_source_dir_app(raw_dir: Path):
    """最小 FastAPI app:仅挂 admin_routes(供 Task 1 测试)."""
    from fastapi import FastAPI
    app = FastAPI()
    app.state.raw = RawStore(raw_dir)
    app.include_router(router, dependencies=[Depends(get_raw(raw_dir))])
    return app


@router.get("/api/sources")
def list_sources():
    r = router
    # raw 通过依赖注入;为简洁,Tesk 实现见下方带 raw 版本
    return {"raw": "injected"}


# 实际实现(带 raw 依赖注入)
def _build_routes(raw_dir: Path):
    """返回挂好 raw 依赖的路由集合(实现期用)."""
    raw = RawStore(raw_dir)

    @router.post("/api/upload")
    async def upload(file: UploadFile):
        if not is_supported(file.filename or ""):
            raise HTTPException(status_code=400, detail="仅支持 PDF/文本")
        data = await file.read()
        if file.filename.endswith((".txt", ".md")):
            from senza_knowledge_mcp.ingest.parser import ParseResult
            from senza_knowledge_mcp.ingest.raw_store import RawStore as RS
            parse = ParseResult(document_markdown=data.decode("utf-8", errors="ignore"), images=[])
        else:  # pdf
            parse = create_parser("docling").parse(Path(file.filename))
        # 文本直接入;PDF 需先落临时文件给 Docling
        raise HTTPException(status_code=501, detail="实现中")

    return upload, list_sources
```

> **重要**:Task 1 上述拼接实现较绕(依赖注入 + 闭包易错)。更简洁做法:让 `create_app` 持有 `raw_dir`,路由用 FastAPI `Depends` 从 app.state 取。为降低风险,Task 1 只实现**纯函数** `is_supported` + 内存/临时 RawStore 下的 `list_sources/get_source`(不依赖 app 注入),上传逻辑(需 Docling)放 Task 2 在 create_app 内完成。Task 1 的可测单元:is_supported、list_sources 数据组装、get_source 数据组装(接受 raw + source_id 入参)。

- [ ] **Step 4(简化后): 重写测试与实现为纯函数**

```python
# admin_routes.py 实际定稿:
from fastapi import APIRouter
from senza_knowledge_mcp.ingest.raw_store import RawStore

router = APIRouter()

def is_supported(filename: str) -> bool: ...   # 见上

def sources_json(raw: RawStore) -> list[dict]:
    return [s.to_dict() for s in raw.list()]

def source_json(raw: RawStore, source_id: str) -> dict:
    if not raw.exists(source_id):
        raise KeyError(source_id)
    doc = raw.read_document(source_id)
    meta = raw.read_source(source_id).to_dict()
    meta["document"] = doc
    return meta
```

> 最终定稿以"纯函数 + router 薄壳(从 fastapi 依赖注入 raw)"为准。写实现时优先最简单正确的形态,测试断言纯函数行为。

- [ ] **Step 5: 验证 + 提交**

```bash
.venv/bin/python -m pytest tests/test_admin_routes.py -v
git add src/senza_knowledge_mcp/admin_routes.py tests/test_admin_routes.py
git commit -m "feat(M4): admin routes (upload + list + detail)"
```

---

### Task 2: admin_app.py(create_app + 页面 + 上传入库)

**Files:**
- Create: `src/senza_knowledge_mcp/admin_app.py`
- Modify: `tests/test_admin_routes.py`(调整为用 create_app)

**Interfaces:**
- Consumes: `admin_routes.router`、`RawStore`、`create_parser("docling")`、`Settings`(M3)
- Produces:
  - `def create_app(raw_dir: Path) -> FastAPI`
  - `def main() -> None` — uvicorn.run(host="127.0.0.1", port=8081)
  - 页面:`GET /`(入口)、`GET /upload`、`GET /sources`、`GET /sources/{id}`
  - `python -m senza_knowledge_mcp.admin_app` 启动

- [ ] **Step 1: 更新测试为用 create_app(真实 PDF 上传走 Docling,用一个极简 PDF 或仅文本)**

```python
# tests/test_admin_routes.py 增补
from senza_knowledge_mcp.admin_app import create_app

def test_create_app_page(tmp_path):
    app = create_app(tmp_path / "raw")
    c = TestClient(app)
    r = c.get("/sources")
    assert r.status_code == 200
    assert "text/html" in r.headers["content-type"] or "html" in r.text.lower()

def test_upload_via_create_app(tmp_path):
    app = create_app(tmp_path / "raw")
    c = TestClient(app)
    r = c.post("/api/upload", files={"file": ("hi.txt", b"# Hi\n\ndiffraction", "text/plain")})
    assert r.status_code == 200
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_admin_routes.py -v`
Expected: FAIL(NoneType / import error 因 create_app 未实现)

- [ ] **Step 3: 写 create_app**

```python
# src/senza_knowledge_mcp/admin_app.py
"""管理后台 FastAPI 应用:上传入库 + 知识库查看(轻量 HTML)."""
from __future__ import annotations

import tempfile
from pathlib import Path

import uvicorn
from fastapi import Depends, FastAPI, Request, UploadFile
from fastapi.responses import HTMLResponse

from senza_knowledge_mcp.admin_routes import is_supported, router as api_router
from senza_knowledge_mcp.ingest import create_parser
from senza_knowledge_mcp.ingest.raw_store import RawStore
from senza_knowledge_mcp.config import load_settings

_PAGE = """<!doctype html><html><head><meta charset="utf-8"><title>领域知识库管理</title></head>
<body><h1>领域知识库</h1>
<ul><li><a href="/upload">上传文档</a></li><li><a href="/sources">知识库列表</a></li></ul>
{body}</body></html>"""


def _raw_dep(request: Request) -> RawStore:
    return request.app.state.raw


def _store_upload(file: UploadFile, raw: RawStore) -> dict:
    data = file.read()  # sync 简化;MVP 不异步
    name = file.filename or "upload"
    suffix = Path(name).suffix.lower()
    if suffix in (".txt", ".md"):
        from senza_knowledge_mcp.ingest.parser import ParseResult
        parse = ParseResult(document_markdown=data.decode("utf-8", errors="ignore"), images=[])
    else:  # .pdf
        with tempfile.NamedTemporaryFile(suffix=".pdf", delete=False) as tmp:
            tmp.write(data)
            tmppath = tmp.name
        try:
            parse = create_parser("docling").parse(Path(tmppath))
        finally:
            Path(tmppath).unlink(missing_ok=True)
    return raw.store(name, data, parse).to_dict()


def create_app(raw_dir: Path) -> FastAPI:
    app = FastAPI(title="senza-knowledge-mcp admin")
    app.state.raw = RawStore(raw_dir)
    app.include_router(api_router, dependencies=[Depends(_raw_dep)])

    @app.post("/api/upload")
    async def upload(request: Request, file: UploadFile):
        if not is_supported(file.filename or ""):
            from fastapi import HTTPException
            raise HTTPException(status_code=400, detail="仅支持 PDF/文本")
        return _store_upload(file, request.app.state.raw)

    @app.get("/", response_class=HTMLResponse)
    def index():
        return _PAGE.format(body="<p>选择一个操作。</p>")

    @app.get("/upload", response_class=HTMLResponse)
    def upload_page():
        form = ('<form method="post" action="/api/upload" enctype="multipart/form-data">'
                '<input type="file" name="file"><button>上传</button></form>')
        return _PAGE.format(body=form)

    @app.get("/sources", response_class=HTMLResponse)
    def sources_page(request: Request):
        raw = request.app.state.raw
        items = [f'<li><a href="/sources/{s.source_id}">{s.name} ({s.source_id})</a></li>' for s in raw.list()]
        return _PAGE.format(body="<ul>" + "".join(items) + "</ul>")

    @app.get("/sources/{source_id}", response_class=HTMLResponse)
    def source_page(request: Request, source_id: str):
        raw = request.app.state.raw
        body = "<pre>" + raw.read_document(source_id) + "</pre>"
        return _PAGE.format(body=body)

    return app


def main() -> None:
    settings = load_settings()
    app = create_app(settings.raw_dir)
    uvicorn.run(app, host="127.0.0.1", port=8081)


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 跑测试**

Run: `.venv/bin/python -m pytest tests/test_admin_routes.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/senza_knowledge_mcp/admin_app.py tests/test_admin_routes.py
git commit -m "feat(M4): admin_app FastAPI (upload + pages + view)"
```

---

### Task 3: verify_m4.py(全链路 MVP)

**Files:**
- Create: `tests/verify_m4.py`

**Interfaces:**
- Consumes: `create_app`(Task 2)、`RawStore`
- Produces: 验证脚本

- [ ] **Step 1: 写脚本**

```python
# tests/verify_m4.py
"""MVP: 启动管理后台,上传真实 PDF,查看知识库。"""
import time
from pathlib import Path
import sys

from fastapi.testclient import TestClient

from senza_knowledge_mcp.admin_app import create_app


def main(pdf: str, raw_root: str) -> None:
    app = create_app(Path(raw_root))
    c = TestClient(app)
    # 上传真实 PDF
    with open(pdf, "rb") as f:
        content = f.read()
    r = c.post("/api/upload", files={"file": (Path(pdf).name, content, "application/pdf")})
    assert r.status_code == 200, r.text
    info = r.json()
    print(f"[1] 上传成功: {info['source_id']} ({info['origin']})")
    # 列表
    r2 = c.get("/api/sources")
    items = r2.json()
    assert any(s["source_id"] == info["source_id"] for s in items), "列表应含新上传"
    print(f"[2] 知识库 {len(items)} 个 source")
    # 查看内容
    r3 = c.get(f"/api/sources/{info['source_id']}")
    assert r3.status_code == 200
    doc = r3.json()["document"]
    print(f"[3] source 内容 {len(doc)} chars")
    print("VERIFY: M4 MVP OK")


if __name__ == "__main__":
    main(sys.argv[1], sys.argv[2] if len(sys.argv) > 2 else ".data/raw")
```

- [ ] **Step 2: 运行**

```bash
cd /Users/hhl/Documents/projs/oh-my-harness/senza-knowledge-mcp
.venv/bin/python tests/verify_m4.py ~/Documents/projs/minerU/pdfs/CurvilinearMaskOverview.pdf .data/raw
```
Expected: 上传成功、列表含新 source、查看内容 OK

- [ ] **Step 3: 全测试 + 提交**

```bash
.venv/bin/python -m pytest tests -v
git add tests/verify_m4.py
git commit -m "feat(M4): full-chain MVP verify (upload PDF -> view KB)"
```

---

## Self-Review

**Spec coverage:**
- 上传入库存:Task 1(文本)+ Task 2(create_app 完整,含 PDF Docling)✔
- 查看知识库:Task 1 `sources_json`/`source_json` + Task 2 页面 ✔
- REST 底座 + 轻量 HTML 页面:Task 2 ✔
- 独立进程 + main():Task 2 ✔
- 复用 RawStore/Docling/Settings:各 Task ✔

**Placeholder 扫描:** 无 TBD/TODO。Task 1 有"实现中 501"——这是**计划中对注入方式的简化说明**,不应留在最终代码;计划明确"最终定稿以纯函数 + router 薄壳为准",Task 1 Step 4 给出了定稿纯函数形态。实现时按纯函数,不留 501。

**类型一致性:** `RawStore.list()/read_document()/store()`(M1)贯穿;`create_app(raw_dir)`、`is_supported(filename)`、`sources_json(raw)`、`source_json(raw, id)` 跨任务一致。✔

**风险提示:** Task 2 的 `_store_upload` 用同步 `file.read()`,fastapi UploadFile 是 async——实现时用 `await file.read()`,路由 async。计划中 sync 写法是示意,实现者以 fastapi 实际语义为准(路由 async def + await file.read())。
